public class CentralProcessingUnit {

    private static int programCounter;
    //get instruction (Load)
    //decode   (decodeInstruction();)
    //execute instruction(executeInstruction();)
    //send to memory, if needed (Store)
    //go to next instruction( PC+4 )

}